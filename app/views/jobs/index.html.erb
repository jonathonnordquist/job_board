<%= provide(:title, formatted_title("All Jobs")) %>

<h1>All available jobs</h1>

<ul>
  <% @jobs.each do |job| %>
    <%= link_to job_path(job.id) do %>
      <div class="job-cell">
        <%= job.title %>
      </div>
    <% end %>
  <% end %>
  
</ul>